{
  "imageDecryptEval": "(function() { const html = _encryptedString; const scriptRegex = /<script[^>]*>([\s\S]*?)<\/script>/g; let match; let imageScriptContent = ''; while ((match = scriptRegex.exec(html)) !== null) { const scriptHtml = match[1]; if (scriptHtml.includes('LoadNextPages') && scriptHtml.includes('new Array()')) { imageScriptContent = scriptHtml; break; } } if (!imageScriptContent) { return JSON.stringify([]); } const arrayNameMatch = imageScriptContent.match(/dTfnT\\(\\d+,\\d+,\\d+,\\d+,\\d+,(_[A-Za-z0-9]+)\\s*,/); const imageArrayName = arrayNameMatch ? arrayNameMatch[1] : null; if (!imageArrayName) { return JSON.stringify([]); } let result = []; try { eval(imageScriptContent); result = eval(imageArrayName); } catch (e) { } return JSON.stringify(result); })();",
  "postDecryptEval": "function atob(input) { const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/='; let str = String(input).replace(/=+$/, ''); if (str.length % 4 === 1) { throw new Error('\'atob\' failed: The string to be decoded is not correctly encoded.'); } let output = ''; for (let bc = 0, bs = 0, buffer, i = 0; buffer = str.charCodeAt(i++); ~buffer && (bs = bc % 4 ? bs * 64 + buffer : buffer, bc++ % 4) ? output += String.fromCharCode(255 & bs >> (-2 * bc & 6)) : 0) { buffer = chars.indexOf(buffer); } return output; } const imageServer = _useServer2 ? \"https://img1.whatsnew247.net/pic\" : \"https://2.bp.blogspot.com\"; const decryptedLinks = _decryptedLinks.map(link => { let mutableLink = link.replace(/pw_.g28x/g, 'b').replace(/d2pr.x_27/g, 'h'); if (!mutableLink.startsWith(\"https\")) { const qualitySuffix = mutableLink.substring(mutableLink.indexOf('?')); const isS0 = mutableLink.includes(\"=s0?\" ); const sIndex = isS0 ? mutableLink.indexOf(\" =s0?\" ) : mutableLink.indexOf(\" =s1600?\" ); let imageId = mutableLink.substring(0, sIndex); imageId = imageId.substring(15, 33) + imageId.substring(50); const idLength = imageId.length; imageId = imageId.substring(0, idLength - 11) + imageId[idLength - 2] + imageId[idLength - 1]; const decodedId = decodeURIComponent(atob(imageId)); let finalId = decodedId.substring(0, 13) + decodedId.substring(17); finalId = finalId.substring(0, finalId.length - 2) + (isS0 ? \"=s0\" : \"=s1600\"); mutableLink = `${imageServer}/${finalId}${qualitySuffix}${_useServer2 ? '&t=10' : ''}`; } return mutableLink; }); JSON.stringify(decryptedLinks);",
  "shouldVerifyLinks": false
}
