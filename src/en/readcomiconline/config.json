{
  "imageDecryptEval": "const pageLinks = []; const dTfnTRegex = /dTfnT\\([^,]+,[^,]+,[^,]+,[^,]+,[^,]+,(_[^\\s,]+)\\s*,\\s*[^,]+,\\s*['\"]([^'\"]+)['\"]/g; const matches = [..._encryptedString.matchAll(dTfnTRegex)]; function atob(t) { const e = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/='; let s = String(t).replace(/=+$/, ''); if (s.length % 4 === 1) throw new Error('atob failed'); let n = ''; for (let t = 0, r, c, i = 0; c = s.charAt(i++); ~c && (r = t % 4 ? r * 64 + c : c, t++ % 4) ? n += String.fromCharCode(255 & r >> (-2 * t & 6)) : 0) c = e.indexOf(c); return n; } function decodeUtf8(s) { const bytes = []; for (let i = 0; i < s.length; i++) bytes.push(s.charCodeAt(i) & 255); let result = ''; let i = 0; while (i < bytes.length) { const b = bytes[i]; if (b < 128) { result += String.fromCharCode(b); i++; } else if ((b & 224) === 192 && i + 1 < bytes.length) { result += String.fromCharCode(((b & 31) << 6) | (bytes[i + 1] & 63)); i += 2; } else if ((b & 240) === 224 && i + 2 < bytes.length) { result += String.fromCharCode(((b & 15) << 12) | ((bytes[i + 1] & 63) << 6) | (bytes[i + 2] & 63)); i += 3; } else if ((b & 248) === 240 && i + 3 < bytes.length) { const code = ((b & 7) << 18) | ((bytes[i + 1] & 63) << 12) | ((bytes[i + 2] & 63) << 6) | (bytes[i + 3] & 63); if (code > 65535) { const high = ((code - 65536) >> 10) + 55296; const low = ((code - 65536) & 1023) + 56320; result += String.fromCharCode(high, low); } else { result += String.fromCharCode(code); } i += 4; } else { i++; } } return result; } function step1(s) { return s.substring(15, 33) + s.substring(50); } function step2(s) { return s.substring(0, s.length - 11) + s.slice(-2); } function decryptLink(t) { let e = t.replace(/Vd__xGEvMo_/g, 'g').replace(/b/g, 'pw_.g28x').replace(/h/g, 'd2pr.x_27').replace(/pw_.g28x/g, 'b').replace(/d2pr.x_27/g, 'h'); if (!e.startsWith('https')) { const qIdx = e.indexOf('?'); const query = qIdx > -1 ? e.substring(qIdx) : ''; const hasS0 = e.includes('=s0?'); const endIdx = hasS0 ? e.indexOf('=s0?') : e.indexOf('=s1600?'); let enc = e.substring(0, endIdx > -1 ? endIdx : e.length); enc = step1(enc); enc = step2(enc); const decoded = atob(enc); const result = decodeUtf8(decoded); const cleaned = result.substring(0, 13) + result.substring(17); const final = cleaned.slice(0, -2) + (hasS0 ? '=s0' : '=s1600'); e = `https://2.bp.blogspot.com/${final}${query}`; } return e; } matches.forEach(m => { if (m[2] && m[2].length > 12) { try { const extracted = m[2].substring(12); const link = decryptLink(extracted); if (link && link.startsWith('http')) pageLinks.push(link); } catch (err) { } } }); JSON.stringify(pageLinks);",
  "postDecryptEval": null,
  "shouldVerifyLinks": false
}
